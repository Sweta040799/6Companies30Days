 string amendSentence (string s)
    {
        string ans = "";
        for(int i=0;i<s.size();i++){
            if(isupper(s[i])){
                if(i > 0)
                ans += " ";
                s[i] = tolower(s[i]);
            }
              ans += s[i];
        }
        
        return ans;
    }
